fn main() {
    // Integer type
    // Floating-point type
    
    // Boolean type
    let t = true;
    let f: bool = false; // Explicit type annotation
    println!("Boolean type -> True: {t} and False: {f}");

    // Character type: 4-Byte unicode Value
    let c = 'z';
    let uc: char = 'µ';
    println!("Character  type -> ASCII: {c} and Unicode: {uc}");
}
